{
  "name": "@brand-system/training-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Training and enablement document generator for Brand System Service clients",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo",
    "dev": "tsc --watch",
    "test": "jest"
  },
  "dependencies": {
    "eta": "^3.0.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
