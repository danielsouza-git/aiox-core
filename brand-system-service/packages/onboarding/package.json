{
  "name": "@brand-system/onboarding",
  "version": "0.1.0",
  "private": true,
  "description": "Onboarding intake flow for Brand System Service — multi-step form handler with ClickUp integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@bss/core": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
